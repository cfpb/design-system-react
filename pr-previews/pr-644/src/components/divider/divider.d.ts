import { HTMLProps, ReactElement } from '../../../node_modules/react';
export declare const Divider: ({ className, ...properties }: HTMLProps<HTMLDivElement>) => ReactElement;
export default Divider;
