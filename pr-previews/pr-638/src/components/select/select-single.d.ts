import { JSX } from '../../../node_modules/react';
import { SelectProperties } from './select';
export declare const SelectSingle: ({ id, options, label, onChange, value, defaultOptionLabel, ...properties }: SelectProperties) => JSX.Element;
